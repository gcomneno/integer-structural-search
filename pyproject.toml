[project]
name = "integer-structural-search"
version = "0.1.0"
description = "Bounded structural search engine for integer payloads"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
